Generated installer artifacts are stored in this directory.

Expected artifacts:
- Windows: `.exe` installer (CPack/NSIS if available) and `.zip` portable archive.
- Linux: `.deb`, `.rpm`, `.tar.gz` packages.

Integrity files:
- `SHA256SUMS.txt` is generated by `scripts/generate_checksums.sh` or `scripts/generate_checksums.ps1`.
